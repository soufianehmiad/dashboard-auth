# MediaStack Dashboard - Environment Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# JWT Authentication
# =============================================================================
# Secret key for signing JWT tokens (REQUIRED)
# Generate a strong random string: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# =============================================================================
# PostgreSQL Database Configuration (REQUIRED)
# =============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=dashboard
POSTGRES_USER=dashboard_app
POSTGRES_PASSWORD=change-this-secure-password

# =============================================================================
# Redis Cache Configuration (REQUIRED)
# =============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=optional-redis-password  # Uncomment if Redis requires auth

# =============================================================================
# CSRF Protection (Optional)
# =============================================================================
# Secret for CSRF token generation (auto-generated if not set)
# Generate: openssl rand -base64 32
# CSRF_SECRET=your-csrf-secret-key

# =============================================================================
# Application Configuration
# =============================================================================
NODE_ENV=production
PORT=3000

# =============================================================================
# Service API Keys (Optional - enables status monitoring)
# =============================================================================
# API keys for *arr services and media management tools
# These enable real-time status monitoring and activity tracking
# Find API keys in each service's Settings → General → Security → API Key

# Content Management Services
SONARR_API_KEY=your-sonarr-api-key-here
SONARR_ANIME_API_KEY=your-sonarr-anime-api-key-here
RADARR_API_KEY=your-radarr-api-key-here
LIDARR_API_KEY=your-lidarr-api-key-here
PROWLARR_API_KEY=your-prowlarr-api-key-here

# Download Clients
QBITTORRENT_API_KEY=your-qbittorrent-api-key-here
SABNZBD_API_KEY=your-sabnzbd-api-key-here

# Analytics & Monitoring
TAUTULLI_API_KEY=your-tautulli-api-key-here

# =============================================================================
# Service URLs (Optional - if different from defaults)
# =============================================================================
# These are usually auto-configured based on service names in Docker network
# Only set these if your services use non-standard URLs

# SONARR_URL=http://sonarr:8989
# SONARR_ANIME_URL=http://sonarr-anime:8990
# RADARR_URL=http://radarr:7878
# LIDARR_URL=http://lidarr:8686
# PROWLARR_URL=http://prowlarr:9696
# QBITTORRENT_URL=http://qbittorrent:8080
# SABNZBD_URL=http://sabnzbd:8080
# TAUTULLI_URL=http://tautulli:8181

# =============================================================================
# Security Notes
# =============================================================================
# 1. NEVER commit this file with real values to version control
# 2. Use strong, unique passwords for PostgreSQL
# 3. Generate strong random values for JWT_SECRET and CSRF_SECRET
# 4. Keep API keys secure - they grant full access to your services
# 5. Change default admin password (admin/Admin123!) immediately after first login

# =============================================================================
# Quick Setup Guide
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Generate JWT secret: openssl rand -base64 32
# 3. Update PostgreSQL credentials (match your docker-compose or local setup)
# 4. Update Redis credentials if needed
# 5. Add API keys from your services (optional, for status monitoring)
# 6. Save and restart: docker restart dashboard-auth
